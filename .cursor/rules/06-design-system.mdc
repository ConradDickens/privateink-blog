---
alwaysApply: true
---

# 设计系统与样式规则

## 确定性随机设计系统（去同质化）

为避免所有项目看起来相同，使用确定性种子生成唯一设计系统。

### Seed 计算公式
```
seed = sha256(项目名 + 目标网络 + YYYYMM + 合约主文件名)
```

**示例**：
- 项目名：FHEVoting
- 网络：sepolia
- 年月：202510
- 合约：VotingContract.sol
- Seed: `sha256("FHEVotingsepolia202510VotingContract.sol")`

### 设计维度选择

根据 seed 从以下维度选择唯一组合：

#### 1. 设计体系（5 选 1）
- **Material Design 3**：现代 Material You 风格，动态颜色
- **Fluent Design**：Microsoft Fluent 2，亚克力效果
- **Neumorphism**：软 UI，凸起/凹陷视觉效果
- **Glassmorphism**：毛玻璃效果，半透明背景，模糊
- **Minimal**：极简主义，去装饰化，大留白

#### 2. 主题色谱（8 组选 1）

| 组别 | 主色 | 辅色 | 强调色 | 适用场景 |
|-----|------|------|--------|---------|
| A | Indigo (#4F46E5) | Purple (#9333EA) | Pink (#EC4899) | 创意/艺术 |
| B | Blue (#3B82F6) | Cyan (#06B6D4) | Teal (#14B8A6) | 专业/科技 |
| C | Green (#10B981) | Lime (#84CC16) | Yellow (#EAB308) | 健康/环保 |
| D | Orange (#F97316) | Amber (#F59E0B) | Red (#EF4444) | 活力/警示 |
| E | Purple (#A855F7) | Deep Purple (#7C3AED) | Indigo (#6366F1) | 奢华/神秘 |
| F | Teal (#14B8A6) | Green (#10B981) | Cyan (#06B6D4) | 清新/自然 |
| G | Red (#EF4444) | Pink (#EC4899) | Orange (#F97316) | 热情/激进 |
| H | Cyan (#06B6D4) | Blue (#3B82F6) | Light Blue (#0EA5E9) | 冷静/理性 |

#### 3. 排版系统（3 选 1）

**Serif - 优雅传统**
- 字体：Georgia, Playfair Display, Merriweather
- 字号倍率：1.2
- 适用：金融、法律、文化

**Sans-Serif - 现代清晰**
- 字体：Inter, Roboto, Open Sans
- 字号倍率：1.25
- 适用：科技、商务、通用

**Monospace - 技术极客**
- 字体：Fira Code, JetBrains Mono, IBM Plex Mono
- 字号倍率：1.15
- 适用：开发工具、区块链、技术

#### 4. 布局模式（5 选 1）

- **Sidebar**：左侧边栏 + 主内容区
- **Masonry**：瀑布流卡片布局
- **Tabs**：顶部标签页导航
- **Grid**：传统 12 栅格系统
- **Wizard**：分步向导流程

#### 5. 组件风格

**圆角**：
- `xs`: 2px
- `sm`: 4px
- `md`: 8px
- `lg`: 12px
- `xl`: 16px
- `2xl`: 24px

**阴影**：
- `none`: 无阴影
- `sm`: `0 1px 2px rgba(0,0,0,0.05)`
- `md`: `0 4px 6px rgba(0,0,0,0.1)`
- `lg`: `0 10px 15px rgba(0,0,0,0.15)`
- `xl`: `0 20px 25px rgba(0,0,0,0.2)`

**边框**：
- 发丝：0.5px
- 细：1px
- 中：1.5px
- 粗：2px

#### 6. 动效时长（3 选 1）

- **快速**：100ms - 适合微交互
- **标准**：200ms - 通用过渡
- **平滑**：300ms - 大型动画

包含：
- 悬停状态
- 加载骨架屏/脉冲
- 页面过渡

## Design Tokens 文件

必须创建 `design-tokens.ts`：

```typescript
import crypto from 'crypto';

// 计算确定性 seed
const projectName = "FHEVoting";
const network = "sepolia";
const yearMonth = "202510";
const contractName = "VotingContract.sol";
const seedString = `${projectName}${network}${yearMonth}${contractName}`;
const seed = crypto.createHash('sha256').update(seedString).digest('hex');

// 根据 seed 选择设计维度（示例）
const seedNum = parseInt(seed.substring(0, 8), 16);
const designSystem = ['Material', 'Fluent', 'Neumorphism', 'Glassmorphism', 'Minimal'][seedNum % 5];
const colorScheme = seedNum % 8; // 0-7 对应 A-H

export const designTokens = {
  system: designSystem,
  seed: seed,
  
  colors: {
    light: {
      primary: '#3B82F6',
      secondary: '#06B6D4',
      accent: '#14B8A6',
      background: '#FFFFFF',
      surface: '#F8FAFC',
      text: '#0F172A',
      textSecondary: '#64748B',
    },
    dark: {
      primary: '#60A5FA',
      secondary: '#22D3EE',
      accent: '#2DD4BF',
      background: '#0F172A',
      surface: '#1E293B',
      text: '#F8FAFC',
      textSecondary: '#94A3B8',
    },
  },
  
  typography: {
    fontFamily: {
      sans: ['Inter', 'system-ui', 'sans-serif'],
      mono: ['JetBrains Mono', 'monospace'],
    },
    scale: 1.25,
    sizes: {
      xs: '0.75rem',    // 12px
      sm: '0.875rem',   // 14px
      base: '1rem',     // 16px
      lg: '1.25rem',    // 20px
      xl: '1.563rem',   // 25px
      '2xl': '1.953rem', // 31px
      '3xl': '2.441rem', // 39px
    },
  },
  
  spacing: {
    unit: 8, // 基础间距单位 8px
  },
  
  borderRadius: {
    sm: '4px',
    md: '8px',
    lg: '12px',
    xl: '16px',
  },
  
  shadows: {
    sm: '0 1px 2px rgba(0,0,0,0.05)',
    md: '0 4px 6px rgba(0,0,0,0.1)',
    lg: '0 10px 15px rgba(0,0,0,0.15)',
  },
  
  transitions: {
    duration: 200,
    easing: 'cubic-bezier(0.4, 0, 0.2, 1)',
  },
  
  layout: 'sidebar', // 'sidebar' | 'masonry' | 'tabs' | 'grid' | 'wizard'
  
  density: {
    compact: {
      padding: { sm: '4px 8px', md: '8px 16px', lg: '12px 24px' },
      gap: '8px',
    },
    comfortable: {
      padding: { sm: '8px 16px', md: '16px 24px', lg: '20px 32px' },
      gap: '16px',
    },
  },
};
```

## Tailwind CSS 集成

如使用 Tailwind，同步到 `tailwind.config.ts`：

```typescript
import { designTokens } from './design-tokens';

export default {
  content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        primary: designTokens.colors.light.primary,
        secondary: designTokens.colors.light.secondary,
        accent: designTokens.colors.light.accent,
      },
      fontFamily: designTokens.typography.fontFamily,
      fontSize: designTokens.typography.sizes,
      borderRadius: designTokens.borderRadius,
      boxShadow: designTokens.shadows,
      transitionDuration: {
        DEFAULT: `${designTokens.transitions.duration}ms`,
      },
    },
  },
};
```

## 禁止硬编码

❌ **错误示例**：
```tsx
<div className="bg-blue-500 text-white rounded-lg shadow-md p-4">
```

✅ **正确示例**：
```tsx
<div className="bg-primary text-on-primary rounded-md shadow-md p-md">
```

## 响应式设计（三档）

```typescript
export const breakpoints = {
  mobile: '0px',      // < 768px
  tablet: '768px',    // 768px - 1024px
  desktop: '1024px',  // > 1024px
};
```

## 无障碍（WCAG AA）

### 对比度要求
- 正常文本：≥ 4.5:1
- 大文本（18pt+）：≥ 3:1
- UI 组件：≥ 3:1

### 焦点指示
```css
.interactive:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
}
```

## 首次回复输出主题摘要

开始新项目时，必须输出：

```markdown
## 🎨 设计系统选型

**设计体系**：Glassmorphism  
**色彩方案**：B 组 (Blue/Cyan/Teal) - 专业科技风格  
**排版系统**：Sans-Serif (Inter) - 1.25 倍率  
**布局模式**：Sidebar（左侧边栏）  
**组件风格**：中圆角(8px) + 中阴影 + 细边框(1px)  
**动效时长**：标准 (200ms)  

**Seed**: `a3f2b8...` (基于 FHEVoting-sepolia-202510-VotingContract.sol)
```
