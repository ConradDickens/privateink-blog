---
alwaysApply: true
---

# FHEVM 强约束

## 合约必须使用 FHEVM 原生能力

参考 [Fhevm0.8_Reference.md](mdc:Fhevm0.8_Reference.md) 和 [FHECounter.sol](mdc:fhevm-hardhat-template/contracts/FHECounter.sol)

### 加密类型
必须使用 FHEVM 提供的加密类型：
```solidity
euint8, euint16, euint32, euint64, euint128, euint256
ebool, eaddress
```

### FHE 运算
必须使用 FHE 库的运算方法：
```solidity
FHE.add(a, b)      // 加法
FHE.sub(a, b)      // 减法
FHE.mul(a, b)      // 乘法
FHE.div(a, b)      // 除法
FHE.eq(a, b)       // 等于
FHE.ne(a, b)       // 不等于
FHE.lt(a, b)       // 小于
FHE.lte(a, b)      // 小于等于
FHE.gt(a, b)       // 大于
FHE.gte(a, b)      // 大于等于
FHE.and(a, b)      // 与
FHE.or(a, b)       // 或
FHE.xor(a, b)      // 异或
FHE.not(a)         // 非
FHE.min(a, b)      // 最小值
FHE.max(a, b)      // 最大值
FHE.select(c, a, b) // 三元选择
```

### 加密输入处理
使用 `inEuintXX` 结构体接收前端加密数据：
```solidity
function processValue(inEuint32 calldata encryptedInput) public {
    euint32 value = FHE.asEuint32(encryptedInput);
    // 处理加密值...
}
```

### 访问控制
必须正确实现访问控制：
```solidity
// 授权给特定地址
FHE.allow(encryptedValue, userAddress);

// 授权给合约自身
FHE.allowThis(encryptedValue);

// 临时授权（仅当前交易有效）
FHE.allowTransient(encryptedValue, userAddress);

// 检查权限
require(FHE.isSenderAllowed(encryptedValue), "Not allowed");
```

### 随机数生成
优先使用 FHEVM 内置随机数：
```solidity
euint32 randomValue = FHE.randEuint32();
```

### 严格禁止
- ❌ 简化 FHEVM 功能为明文运算
- ❌ 降级加密类型为普通类型
- ❌ 自创加密/解密方案
- ❌ 跳过访问控制步骤
- ❌ 使用伪造的 FHE API

## 前端与合约交互约束

### 仅可二选一

**选项 1：真实 Relayer**
- 使用 `@zama-fhe/relayer-sdk`
- 适用于测试网和主网
- 参考 [RelayerSDKLoader.ts](mdc:frontend/fhevm/internal/RelayerSDKLoader.ts)

**选项 2：本地 Mock**
- 使用 `@fhevm/mock-utils`
- 仅适用于本地开发（chainId=31337）
- 参考 [fhevmMock.ts](mdc:frontend/fhevm/internal/mock/fhevmMock.ts)

### 严格禁止
- ❌ 第三种交互方式
- ❌ 伪造 SDK 或自定义加密逻辑
- ❌ 混用 Relayer 和 Mock（必须根据 chainId 动态切换）

## 必须参考的文件（只读）

开发前必须先读取并参考以下文件，但不得直接复制：

### FHEVM 核心逻辑
- [frontend/fhevm/internal/fhevm.ts](mdc:frontend/fhevm/internal/fhevm.ts)
- [frontend/fhevm/internal/RelayerSDKLoader.ts](mdc:frontend/fhevm/internal/RelayerSDKLoader.ts)
- [frontend/fhevm/internal/constants.ts](mdc:frontend/fhevm/internal/constants.ts)
- [frontend/fhevm/internal/mock/fhevmMock.ts](mdc:frontend/fhevm/internal/mock/fhevmMock.ts)

### React Hooks
- [frontend/hooks/useFHECounter.tsx](mdc:frontend/hooks/useFHECounter.tsx)

### 组件
- [frontend/components/FHECounterDemo.tsx](mdc:frontend/components/FHECounterDemo.tsx)

### Providers
- [frontend/app/providers.tsx](mdc:frontend/app/providers.tsx)

### 脚本
- [frontend/scripts/genabi.mjs](mdc:frontend/scripts/genabi.mjs)
- [frontend/scripts/is-hardhat-node-running.mjs](mdc:frontend/scripts/is-hardhat-node-running.mjs)

## 等价实现要求

声称"已参考"时，必须在新前端目录提供等价实现点列表：

| 参考文件 | 职责 | 新项目对应文件 |
|---------|------|---------------|
| `frontend/fhevm/internal/fhevm.ts` | FHEVM 实例管理 | `<new>/fhevm/fhevm.ts` |
| `frontend/fhevm/internal/RelayerSDKLoader.ts` | SDK 动态加载 | `<new>/fhevm/loader.ts` |
| `frontend/hooks/useFHECounter.tsx` | 合约交互 Hook | `<new>/hooks/useContract.tsx` |

必须说明每个新文件实现了哪些关键职责。
