---
alwaysApply: true
---

# ZAMA FHEVM 核心原则

## 语言与本地化

**助手回复语言**：所有聊天回复必须使用简体中文

**项目 UI 语言**：仅使用英语（English-only），不引入 i18n 框架，除非用户明确要求国际化

## 根路径定义与边界

### 根路径 a
在当前工作区查找同时包含以下两个子目录的目录：
- [fhevm-hardhat-template](mdc:fhevm-hardhat-template)
- [frontend](mdc:frontend)

找到的目录即为根路径 a。

**处理规则**：
- 若找到多个候选：列出所有候选路径，一次性询问用户选择
- 若未找到：一次性询问用户提供根路径

### Frontend 目录约束
[frontend](mdc:frontend) 目录是**只读参考目录**：
- ❌ 禁止修改其中任何文件
- ✅ 仅作为实现思路参考

## 反傻护栏（Zero-Fabrication）

### 唯一真实来源
- ✅ 本仓库代码和产物
- ✅ 真实 SDK 官方 API 文档
- ✅ 用户提供的需求文档
- ❌ 任何其他来源

### 严格禁止
- ❌ 输出假地址、假哈希、假接口、假日志、假截图
- ❌ 声称"Sepolia 不支持 FHEVM"
- ❌ 编造不存在的 API 或功能

### 正确做法
- ✅ 缺少产物时，输出可运行的脚本/命令并标注"需运行生成"
- ✅ 部署失败时，从配置/密钥/链状态/脚本/网络排查

## 禁止占位/延期策略

### 绝对禁止
不得交付以下类型的实现：
- ❌ 静态演示页面
- ❌ 仅样式无功能
- ❌ 仅原型/占位符
- ❌ 伪数据/假接口

### 禁用词汇（视为违规）
以下措辞严格禁止使用：
- "静态演示"
- "示例页面"
- "稍后实现"
- "下一步实现"
- "后续补充"
- "先放占位"
- "临时数据"
- "伪数据"
- "假接口"
- "Mock"（`@fhevm/mock-utils` 除外）

### 澄清机制
如需澄清需求，必须：
1. **单次合并提问**：将所有疑问点合并为一次询问
2. 等待用户一次性回复
3. 继续实现

不得多次往返询问琐碎细节。

## 一次性澄清策略

仅当无法自动探测以下关键信息时，发起单次合并提问：
- 根路径位置
- 前端目录名称
- 机密变量（私钥、API Key 等）

提问格式示例：
```
需要以下信息才能继续：
1. 根路径 a：未找到同时包含 fhevm-hardhat-template 和 frontend 的目录，请提供
2. 新前端目录名：需求文档未指定，建议使用 voting-frontend，是否同意？
3. Sepolia 部署密钥：需要 SEPOLIA_PRIVATE_KEY 和 INFURA_API_KEY

请一次性提供后继续。
```

## 搜索与执行策略

### 基本原则
1. **先读仓库，再实现**：不得凭空猜测代码结构
2. **并行读取**：同时读取多个关键文件以提高效率
3. **真实产物为准**：以仓库实际产物为唯一真相来源

### 执行流程
1. 语义检索定位关键代码位置
2. 定点读取相关文件
3. 实现功能
4. 自检验证
5. 提供可运行命令

### 示例
```bash
# 错误：直接猜测部署地址
const contractAddress = "0x1234...";

# 正确：读取部署产物
const deployment = require('../fhevm-hardhat-template/deployments/localhost/FHECounter.json');
const contractAddress = deployment.address;
```
