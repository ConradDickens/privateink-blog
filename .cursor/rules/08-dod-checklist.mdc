---
alwaysApply: true
---

# 完成定义（DoD）与执行前检查清单

## 执行前自检（9 项必查）

开始任何新 dApp 开发前，必须完成以下检查：

### ☐ 1. 根路径 a 确认
- [x] 确认包含 [fhevm-hardhat-template](mdc:fhevm-hardhat-template) 目录
- [x] 确认包含 [frontend](mdc:frontend) 参考目录
- [ ] 如有多个候选，列出并询问用户
- [ ] 如未找到，一次性询问用户

### ☐ 2. 部署产物可用或可生成
- [ ] 检查 `fhevm-hardhat-template/deployments/` 目录
- [ ] 确认存在目标网络的部署文件
- [ ] 如不存在，准备部署命令

### ☐ 3. 新前端目录命名与创建
- [ ] 从需求文档读取目录名
- [ ] 或使用 `<slug>-frontend` 格式
- [ ] 或使用 `fhevm-frontend-<timestamp>`
- [ ] 在根路径 a 下创建目录

### ☐ 4. ABI 生成脚本可用
- [ ] 参考 [genabi.mjs](mdc:frontend/scripts/genabi.mjs)
- [ ] 创建 `scripts/genabi.mjs`
- [ ] 支持从 deployments 读取
- [ ] 生成 TypeScript 类型安全文件

### ☐ 5. dev:mock / dev 两入口存在
- [ ] 配置 `dev:mock` 脚本
- [ ] 配置 `dev` 脚本
- [ ] 创建节点检测脚本
- [ ] 语义与参考项目一致

### ☐ 6. Relayer 注入与 Mock 动态导入逻辑
- [ ] 参考 [RelayerSDKLoader.ts](mdc:frontend/fhevm/internal/RelayerSDKLoader.ts)
- [ ] 参考 [fhevmMock.ts](mdc:frontend/fhevm/internal/mock/fhevmMock.ts)
- [ ] 实现 chainId 检测
- [ ] 实现动态切换逻辑

### ☐ 7. 合约编译/测试通过
- [ ] `npx hardhat compile` 无错误
- [ ] `npx hardhat test` 全部通过
- [ ] 使用正确的 FHEVM API

### ☐ 8. 前端构建通过
- [ ] `npm run build` 成功
- [ ] 无 TypeScript 错误
- [ ] 无 ESLint 错误（如配置）

### ☐ 9. 加密→解密闭环打通
- [ ] 用户输入加密
- [ ] 调用合约上链
- [ ] 获取加密句柄
- [ ] 执行授权操作
- [ ] 用户解密显示

## 完成定义（DoD）- 验收标准

### 合约部分验收

#### ✅ 编译通过
```bash
cd fhevm-hardhat-template
npx hardhat compile
```
**预期输出**：
```
Compiled X Solidity files successfully
```

#### ✅ 测试通过
```bash
npx hardhat test
```
**预期输出**：
```
  ContractName
    ✓ should do something (XXXms)
    ✓ should prevent invalid operation (XXXms)

  X passing (Xs)
```

#### ✅ 本地部署可执行
```bash
# 终端 1：启动节点
npx hardhat node

# 终端 2：部署
npx hardhat deploy --network localhost
```
**预期输出**：
```
deploying "ContractName"...
✓ ContractName deployed at: 0x1234...
```

#### ✅ 测试网部署（需机密）
```bash
npx hardhat deploy --network sepolia
```

**需要的环境变量**：
- `SEPOLIA_PRIVATE_KEY` - 部署账户私钥
- `INFURA_API_KEY` 或 `ALCHEMY_API_KEY`
- 账户有足够的 Sepolia ETH

如缺少，发起一次性询问：
```
需要以下配置才能部署到 Sepolia：
1. SEPOLIA_PRIVATE_KEY - 部署账户私钥
2. INFURA_API_KEY - Infura 项目 API 密钥
3. 确认账户有足够的 Sepolia ETH（至少 0.1 ETH）

请提供后继续。
```

#### ✅ 合约使用 FHEVM 原生能力
- [x] 使用 `euintXX` 等加密类型
- [x] 使用 `FHE.add/sub/mul` 等运算
- [x] 使用 `FHE.asEuintXX(inEuintXX)` 处理输入
- [x] 使用 `FHE.allow/allowThis` 访问控制
- [x] 无简化/降级/伪造

### 前端部分验收

#### ✅ 双模式脚本存在
`package.json` 必须包含：
```json
{
  "scripts": {
    "dev:mock": "node scripts/check-node.mjs && node scripts/genabi.mjs && next dev",
    "dev": "node scripts/genabi.mjs && next dev",
    "build": "next build"
  }
}
```

#### ✅ 构建成功
```bash
cd <new-frontend>
npm run build
```
**预期输出**：
```
✓ Compiled successfully
✓ Collecting page data
✓ Generating static pages
✓ Finalizing page optimization

Build completed successfully
```

#### ✅ 加密→解密闭环验收

**测试流程**：
1. 用户在 UI 输入明文值
2. 前端调用 FHEVM SDK 加密
3. 提交加密数据到合约
4. 合约处理并授权
5. 前端解密并显示结果

**参考实现**：
- [useFHECounter.tsx](mdc:frontend/hooks/useFHECounter.tsx)
- [FHECounterDemo.tsx](mdc:frontend/components/FHECounterDemo.tsx)

#### ✅ UI 英语-only
- [ ] 所有按钮使用英语
- [ ] 所有标签使用英语
- [ ] 所有提示信息使用英语
- [ ] 未引入 i18n 框架

#### ✅ 等价实现点文档

必须提供对照表：

| 参考文件 | 职责 | 新项目文件 | 状态 |
|---------|------|----------|------|
| `frontend/fhevm/internal/fhevm.ts` | FHEVM 实例管理 | `voting-frontend/fhevm/fhevm.ts` | ✅ |
| `frontend/fhevm/internal/RelayerSDKLoader.ts` | SDK 动态加载 | `voting-frontend/fhevm/loader.ts` | ✅ |
| `frontend/fhevm/internal/constants.ts` | 常量配置 | `voting-frontend/fhevm/constants.ts` | ✅ |
| `frontend/hooks/useFHECounter.tsx` | 合约交互 | `voting-frontend/hooks/useVoting.tsx` | ✅ |
| `frontend/components/FHECounterDemo.tsx` | UI 组件 | `voting-frontend/components/VotingUI.tsx` | ✅ |
| `frontend/app/providers.tsx` | Provider | `voting-frontend/app/providers.tsx` | ✅ |
| `frontend/scripts/genabi.mjs` | ABI 生成 | `voting-frontend/scripts/genabi.mjs` | ✅ |

## 自检失败处理

### 处理流程
1. **明确指出**哪个检查项未通过
2. **说明原因**为何未通过
3. **提供解决方案**或询问用户
4. **暂停实现**直到该项通过

### 示例：检查项 2 失败

```
⚠️ 自检失败：检查项 2 - 部署产物不可用

**原因**：
fhevm-hardhat-template/deployments/localhost/ 目录不存在

**解决方案**：

步骤 1 - 启动本地节点（终端 1）：
```bash
cd fhevm-hardhat-template
npx hardhat node
```

步骤 2 - 部署合约（终端 2）：
```bash
cd fhevm-hardhat-template
npx hardhat deploy --network localhost
```

完成后会生成：
- deployments/localhost/FHECounter.json
- deployments/localhost/solcInputs/...

是否已完成部署？
```

### 示例：检查项 7 失败

```
⚠️ 自检失败：检查项 7 - 合约测试未通过

**测试输出**：
```
  FHECounter
    ✓ should initialize
    ✗ should add encrypted values
    Error: Transaction reverted without a reason

  1 passing
  1 failing
```

**可能原因**：
1. 未正确授权加密值
2. 缺少 FHE.allowThis 调用
3. 访问控制逻辑错误

需要检查合约代码的访问控制部分。
```

## 完成后输出摘要

所有检查项通过且实现完成后，输出标准摘要：

```markdown
## ✅ 项目完成

### 自检结果
- ✅ 根路径 a 确认
- ✅ 部署产物可用
- ✅ 新前端目录创建
- ✅ ABI 生成脚本
- ✅ 双模式入口配置
- ✅ Relayer/Mock 逻辑
- ✅ 合约编译/测试通过
- ✅ 前端构建通过
- ✅ 加密→解密闭环

### DoD 验收通过

**合约部分**：
```bash
cd fhevm-hardhat-template
npx hardhat compile && npx hardhat test
# ✅ 编译成功，8 个测试通过
```

**前端部分**：
```bash
cd voting-frontend
npm run build
# ✅ 构建成功
```

### 运行命令

**本地开发**：
```bash
# 终端 1：启动节点
cd fhevm-hardhat-template
npx hardhat node

# 终端 2：部署合约
npx hardhat deploy --network localhost

# 终端 3：启动前端
cd voting-frontend
npm run dev:mock
```

**测试网部署**：
```bash
cd fhevm-hardhat-template
npx hardhat deploy --network sepolia
```

### 等价实现点
（见上表）

项目已就绪！🎉
```
