---
alwaysApply: true
---

# 文档与输出规则

## 严格禁止创建的文档

除非用户明确要求，否则禁止创建以下任何类型的 Markdown 文档：

### ❌ 禁止文档列表
- `fix.md` - 修复记录
- `summary.md` - 总结文档
- `postmortem.md` - 事后分析
- `changelog.md` - 变更日志
- `design.md` - 设计说明
- `architecture.md` - 架构文档
- `implementation.md` - 实现细节
- `notes.md` - 开发笔记
- `todos.md` - 待办事项
- `issues.md` - 问题记录
- `decisions.md` - 决策记录
- `docs/*.md` - docs 目录下的任何文档
- `IMPLEMENTATION_*.md` - 任何实现相关文档
- `DEPLOYMENT_*.md` - 任何部署相关文档

### ✅ README.md 例外
`README.md` 是唯一例外：
- 默认不改写现有 README
- 仅在用户明确要求时更新
- 不主动创建新 README

## 结论写在聊天回复中

所有实现总结、问题分析、决策说明必须直接写在聊天回复中。

### ✅ 正确格式示例

```markdown
## 实现完成

已完成 FHEVoting 合约和前端开发。

### 合约部分
- ✅ VotingContract.sol - 支持加密投票
- ✅ 使用 euint32 存储投票数据
- ✅ 实现 FHE.allow 访问控制
- ✅ 防止重复投票

### 前端部分
- ✅ voting-frontend 目录已创建
- ✅ 加密投票功能
- ✅ 解密结果展示
- ✅ Mock/Relayer 双模式支持

### 验证测试
```bash
cd fhevm-hardhat-template
npx hardhat test
# ✅ 8 passing
```

### 部署命令
```bash
# 启动本地节点（终端 1）
npx hardhat node

# 部署合约（终端 2）
npx hardhat deploy --network localhost

# 启动前端（终端 3）
cd voting-frontend
npm run dev:mock
```

### 等价实现点
| 参考文件 | 新项目文件 | 职责 |
|---------|----------|------|
| frontend/fhevm/internal/fhevm.ts | voting-frontend/fhevm/fhevm.ts | FHEVM 实例管理 |
| frontend/hooks/useFHECounter.tsx | voting-frontend/hooks/useVoting.tsx | 合约交互逻辑 |

项目已就绪！
```

### ❌ 错误做法
创建 `IMPLEMENTATION_SUMMARY.md` 或其他文档文件。

## 输出格式要求

### 使用结构化 Markdown
- 使用清晰的标题层级（##, ###）
- 使用列表和表格
- 使用代码块标注语言
- 突出关键信息（✅ ❌ ⚠️）

### 提供可运行命令
```bash
# 编译合约
cd fhevm-hardhat-template
npx hardhat compile

# 运行测试
npx hardhat test

# 部署
npx hardhat deploy --network localhost
```

### 显示文件结构
```
voting-frontend/
├── app/
│   ├── page.tsx
│   └── providers.tsx
├── components/
│   └── VotingUI.tsx
└── fhevm/
    └── fhevm.ts
```

## 代码注释规范

### Solidity - NatSpec 格式
```solidity
/// @notice 投票给指定候选人
/// @param encryptedVote 加密的投票选项
/// @dev 使用 FHE.asEuint32 解析加密输入
/// @return success 是否成功
function vote(inEuint32 calldata encryptedVote) 
    public 
    returns (bool success) 
{
    // 实现...
}
```

### TypeScript - JSDoc 格式
```typescript
/**
 * 加密用户投票并提交到链上
 * @param vote 明文投票选项 (0-N)
 * @returns 交易哈希
 * @throws 如果用户已投票
 */
async function submitVote(vote: number): Promise<string> {
    // 实现...
}
```

### 行内注释
仅在复杂逻辑处使用：
```typescript
// 根据 chainId 选择 Mock 或真实 Relayer
if (chainId === 31337 && metadata) {
    // 本地开发：使用 Mock
    const { createInstance } = await import('@fhevm/mock-utils');
} else {
    // 测试网/主网：使用真实 Relayer SDK
    const sdk = await loadRelayerSDK();
}
```

## 禁止冗余注释

❌ **错误示例**：
```typescript
// 声明计数器变量
const counter = 0;

// 增加计数器
counter++;

// 返回结果
return result;
```

✅ **正确做法**：
```typescript
const counter = 0;
counter++;
return result;
```

## Git Commit 信息规范

使用 Conventional Commits：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档变更
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具链

### 示例
```
feat(contract): add encrypted voting functionality

- Implement VotingContract with euint32 votes
- Add FHE.allow access control for vote results
- Support multiple candidates with encrypted counts
- Prevent double voting using ebool mapping

Closes #123
```

## 错误处理输出

### 前端错误
```typescript
try {
    await contract.vote(encryptedVote);
    console.log('✅ Vote submitted successfully');
} catch (error) {
    if (error instanceof Error) {
        console.error('❌ Vote failed:', error.message);
        setError('Failed to submit vote. Please try again.');
    }
}
```

### 合约错误
```solidity
error VotingClosed();
error AlreadyVoted();
error Unauthorized();

function vote(inEuint32 calldata encryptedVote) public {
    if (votingEnded) revert VotingClosed();
    if (hasVoted[msg.sender]) revert AlreadyVoted();
    // ...
}
```

## 用户明确要求时的例外

如果用户明确说：
- "创建一个 README"
- "生成文档"
- "写一个设计文档"
- "创建 API 文档"

则可以创建相应文档，但应该：

### 1. 确认用户意图
```
您是否需要我创建完整的 README.md？
请告诉我应该包含哪些章节：
- [ ] 项目介绍
- [ ] 安装步骤
- [ ] 使用指南
- [ ] 部署说明
- [ ] API 文档
- [ ] 常见问题

或者您有其他要求？
```

### 2. 创建符合要求的文档
根据用户确认的需求创建文档。

### 3. 保持简洁
不要过度详细，重点突出关键信息。
